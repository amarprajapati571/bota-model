# Core runtime packages for the live backend/API layer.
#
# The current starter code still runs with the Python standard library, but
# these packages are the expected production runtime stack for live stream
# ingestion, API/WebSocket delivery, metrics, and image-frame processing.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-dotenv>=1.0
PyYAML>=6.0

numpy>=1.26
opencv-python-headless>=4.10
Pillow>=10.4

requests>=2.32
httpx>=0.27
websockets>=12.0
aiofiles>=23.2
python-multipart>=0.0.9

prometheus-client>=0.20
orjson>=3.10
